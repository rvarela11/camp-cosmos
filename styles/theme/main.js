import { createTheme } from '@mui/material/styles';

const mainTheme = createTheme({
  palette: {
    background: { default: "#1F1D2B" },
    text: { primary: "#FFFFFF" },
  },
});

export default mainTheme;
